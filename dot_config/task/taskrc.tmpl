{{- if or (eq .chezmoi.hostname "kditdkenneth") (eq .chezmoi.hostname "dlinken") }}
data.location=~/.local/share/task
{{- else if eq .chezmoi.hostname "localhost" }}
data.location="~/storage/shared/Taskwarrior Data"
{{- end }}
hooks.location=~/.config/task/hooks
editor=nvim
default.project=inbox

context.work.read=+work
context.work.write=+work

context.homeserver.read=+homeserver
context.homeserver.write=+homeserver
news.version=3.3.0
context.note.read=+note
context.note.write=+note
